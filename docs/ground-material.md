# 地面纹理

地面在 Worker 内按世界坐标生成 256×256 RGBA 区块纹理（每米 8 像素）。街道使用 抖动 Voronoi 不规则青石、浅色石缝、局部苔色与逐石色差，非道路区域沿用生态底色并增加细小颗粒。道路边缘平滑混合，跨区块图案连续。

沿用原地形网格、碰撞与道路规划，不添加逐石模型。每区块一张纹理和一个地面材质，使用 mipmap 与 4 倍各向异性过滤；网格销毁时同时释放材质和纹理。单张原始数据 256 KiB（含 mipmap 约 341 KiB），随可视距离加载。

不改变存档版本。遵循 AGENTS.md，未运行测试、build 或 lint。
